const { getRawInput } = require("readline-sync")

s.jfoiea g oiewfjlids
falseg
dg
rae
WebGL2RenderingContextreg
RegExpr
static getDerivedStateFromProps(nextProps, prevState) {
    static getDerivedStateFromProps(nextProps, prevState) {
        getRawInputg
    }
    
}
